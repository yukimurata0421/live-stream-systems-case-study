# stream_v3 host watchdog module options.
#
# nowayout=0 keeps the first rollout reversible. Move to nowayout=1 only after
# a planned hardware-watchdog reboot drill proves the BIOS/driver path.
options iTCO_wdt heartbeat=90 nowayout=0
options softdog soft_noboot=0 soft_panic=0 nowayout=0
