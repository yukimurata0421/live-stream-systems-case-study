[Manager]
# PID1 opens /dev/watchdog and refreshes it. If PID1 or userspace stops making
# progress, the watchdog expires and the host reboots.
RuntimeWatchdogSec=60s

# Keep the existing slow reboot guard during shutdown/reboot.
RebootWatchdogSec=10min
