[Service]
# The monitoring-host copy is an independent comparison observer and must not
# launch delivery-host WAN or RTMPS burst capture helpers.
Environment=WAO_PERSISTENT_TRIGGER_WAN_SNAPSHOT=0
Environment=WAO_PERSISTENT_TRIGGER_RTMPS_BURST=0
