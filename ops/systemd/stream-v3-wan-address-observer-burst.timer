[Unit]
Description=Run stream_v3 high-cadence WAN address observer during the recurring morning TCP stall window

[Timer]
OnCalendar=*-*-* 23:00:00 UTC
AccuracySec=1s
Unit=stream-v3-wan-address-observer-burst.service

[Install]
WantedBy=timers.target
