# Keep Dell recovery observation/decision processes out of unattended needrestart.
# Package installation remains enabled; a bounded planned restart owns activation.
$nrconf{override_rc}{qr(^dell-observation-server@.+\.service$)} = 0;
$nrconf{override_rc}{qr(^dell-recovery-agent-shadow\.service$)} = 0;
